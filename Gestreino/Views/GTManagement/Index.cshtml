@{
    ViewBag.Title = "";
}
<main>
    <div class="row">
        @if (Request.IsAuthenticated)
        {
            <div class="col-md-3">
                @Html.Partial("_SideBarMenu")
            </div>
            <div class="col-md-8">
            </div>
        }
        else
        {
            <div class="col-md-12">
                <div class="mb-5">
                    <p class="text-center"><a href="@Url.Action("login","account")" class="btn btn-primary">Iniciar sessão</a></p>
                </div>
            </div>
        }
    </div>
</main>